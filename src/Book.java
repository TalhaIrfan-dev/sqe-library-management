public class book
{
        String bookTitle;
        String author;
        int copies;

        book(String title, String author, int copies)
        {
            this.bookTitle = title;
            this.author = author;
            this.copies = copies;
        }
}